# Tasky is a fully offline app and requires NO secrets or API keys to run.
#
# This file exists only as a placeholder convention. If you later add an
# optional cloud-sync backend, declare its configuration here, e.g.:
#
# SYNC_API_BASE_URL=https://api.example.com
# SYNC_API_KEY=your-key-here
